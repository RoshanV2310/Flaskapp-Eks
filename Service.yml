apiVersion: v1
kind: Service
metadata:
  name: flaskapp
  namespace: flaskapp
  labels: 
    app: flaskapp
spec:
  type: NodePort
  selector:
    app: flaskapp
  ports: 
    - port: 5000
      targetPort: 5000
      nodePort: 31231
